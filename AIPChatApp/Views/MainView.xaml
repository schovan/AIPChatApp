<Window x:Class="AIPChatApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:AIPChatApp.Behaviors"
        xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
        xmlns:viewModels="clr-namespace:AIPChatApp.ViewModels"
        mc:Ignorable="d"
        Title="AI Chat" Height="700" Width="900"
        Background="#343541" 
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="UserMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#444654"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="40,10,40,10"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="AiMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#444654"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="40,10,40,10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="MessageTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#ECECF1"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#202123" Padding="15">
            <TextBlock Text="Google Gemini Chat" Foreground="White" FontWeight="Bold" FontSize="16"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="ChatScrollViewer">
            <i:Interaction.Behaviors>
                <behaviors:AutoScrollToBottomBehavior ItemsSource="{Binding Messages}" />
            </i:Interaction.Behaviors>
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:MessageViewModel}">
                        <Grid Margin="0,5,0,5">
                            <Border x:Name="UserMessageBorder" 
                                    Style="{StaticResource UserMessageStyle}" 
                                    Visibility="Collapsed"
                                    MaxWidth="600">
                                <TextBlock Text="{Binding Content}" 
                                           Style="{StaticResource MessageTextStyle}"/>
                            </Border>
                            
                            <Border x:Name="AiMessageBorder" 
                                    Style="{StaticResource AiMessageStyle}" 
                                    Visibility="Collapsed"
                                    MaxWidth="600">
                                <markdig:MarkdownViewer Markdown="{Binding Content}" 
                                                        Foreground="#ECECF1" 
                                                        FontSize="14"/>
                            </Border>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                <Setter TargetName="UserMessageBorder" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsUser}" Value="False">
                                <Setter TargetName="AiMessageBorder" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#343541" Padding="20" BorderBrush="#565869" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center" 
                         Background="#40414F" 
                         Foreground="White" 
                         Padding="12"
                         BorderThickness="0"
                         FontSize="14"
                         Height="Auto"
                         AcceptsReturn="True">
                    <i:Interaction.Behaviors>
                        <behaviors:EnterToSendBehavior Command="{Binding SendMessageCommand}" />
                    </i:Interaction.Behaviors>
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <Button Grid.Column="1" 
                        Command="{Binding SendMessageCommand}" 
                        Margin="10,0,0,0"
                        Background="#19c37d" 
                        Foreground="White"
                        Content="Send"
                        Padding="15,8"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Opacity" Value="0.5"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
